// テンプレート文字列
const name = "moritaka";
const age = "40";

const message = `わたしの名前は${name}です。年齢は${age}歳です。`;
console.log(message);

// アロー関数
const func1 = (str) => {
  return str;
};
console.log(func1("allow"));
